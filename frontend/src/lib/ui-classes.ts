/** Default card surface used across the app */
export const cardSurface = 'border-border/50 bg-card/80';

/** Soft gradient card surface (welcome, summary, heroes) */
export const cardSurfaceSoft =
  'border-border/50 bg-gradient-to-br from-card via-card to-secondary/30';

/** Small circular/rect icon well next to titles */
export const iconWell = 'flex size-9 items-center justify-center rounded-lg bg-secondary';

/** Status / score pill base */
export const statusPillBase =
  'inline-flex items-center rounded-full px-2 py-0.5 text-xs font-medium ring-1 ring-inset';

/** Checkbox filter row chrome */
export const checkboxRow =
  'flex items-start gap-3 rounded-xl border border-border/60 bg-secondary/20 p-4 transition-colors hover:bg-secondary/40';

/** Page section title (h2/h3 under the main page header) */
export const sectionTitle = 'text-lg font-semibold tracking-tight';
