import { SettingsView } from "@/features/settings/settings-view";

/**
 * Settings page
 * Thin route — form logic lives in features/settings.
 */
export default function SettingsPage() {
  return <SettingsView />;
}
